# Background Job Scheduler

Build a background job scheduler with a working UI. Jobs get created, queued, processed, and tracked.
Workers run independently. The system handles failure on its own. 
A scheduler that only works in the happy path is broken.
